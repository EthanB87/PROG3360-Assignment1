apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: assignment3
  labels:
    app: product-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      containers:
      - name: product-service
        image: product-service:v2  # Replace with your built image
        imagePullPolicy: Never          # use local image, avoid ImagePullBackOff
        ports:
        - containerPort: 8081
        envFrom:
        - configMapRef:
            name: app-config
